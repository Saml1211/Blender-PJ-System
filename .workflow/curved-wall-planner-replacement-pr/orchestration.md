# Orchestration: Curved Wall Planner Replacement PR

## Execution Rules

- Keep the original objective intact.
- Ask for approval before risky, expensive, external, or destructive actions.
- Keep immediate blocking work local.
- Delegate only bounded, disjoint, materially useful packets.
- Integrate packet results before final verification.

## Branching Rules

- If a formula review finds a plausible defect, first add a production-importing
  regression test; fix only after the test fails for the expected reason.
- If Blender camera framing disagrees with core rays, treat runtime geometry as
  authoritative and reconcile the adapter without changing the pure convention.
- If the archive validates but install still fails with the cookie error, repair
  only the Blender user extension-repository lock/cache implicated by runtime
  inspection; do not alter unrelated extensions or global harness config.
- If deletion review finds unique required functionality, preserve it as a
  documented future requirement or port the smallest necessary Blender-native
  slice; do not restore the vulnerable CRA dependency tree.
- If any required check is red, do not commit/push/open the replacement PR as
  ready and do not close PR #1.

## Packet Prompts

See the three subagent task messages recorded in this conversation. All packets
are read-only and return evidence, defect hypotheses, and concrete missing tests.

## Completion Audit

- Original success criteria re-read against tool evidence.
- Staged paths audited so local skills/config/workflow/logs/zips are excluded.
- Replacement PR exists against `main`, branch is pushed, CI status inspected.
- PR #1 closed only after the replacement is ready; no merge performed.
